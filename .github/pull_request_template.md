## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] `npm run lint` and `npm run build` pass for every package I touched (backend and/or frontend)
- [ ] I manually tested the affected flow against a real mail account
- [ ] I did not commit secrets, `.env` files, or build output (`dist/`, `node_modules/`)
- [ ] Docs (`README.md`, `docs/`) are updated if behavior or setup changed
